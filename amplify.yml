version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm start
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
  artifacts:
    baseDirectory: ./client
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*